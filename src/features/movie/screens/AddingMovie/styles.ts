import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexGrow: 1,
        padding: 10,
    },
    contentContainer: {
        flex: 1
    },
    buttonStyle: {
        borderRadius: 20
    }
});